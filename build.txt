Training a Large Language Model (LLM) From Scratch — Complete Guide
0. Overview

This guide explains how to build and train an LLM from scratch:

Define objective
Collect & prepare dataset
Train tokenizer
Design model architecture
Initialize model
Train (pretraining)
Optimize & scale
Evaluate
Fine-tune
Deploy
1. Define the Objective
1.1 Model Type
General LLM (GPT-style)
Domain-specific (medical, legal, Arabic, etc.)
Multilingual
1.2 Scale Decision
Model Size	Parameters	Use Case
Small (SLM)	100M – 1B	Local / low GPU
Medium	1B – 7B	Production
Large	7B+	Advanced systems
2. Dataset Collection
2.1 Data Sources
Web scraping (Common Crawl)
Wikipedia dumps
Books datasets
Code datasets (GitHub)
Conversations datasets
2.2 Dataset Types
Raw text
Instruction data
Dialogue data
2.3 Scaling
Minimum viable: 10B tokens
Strong model: 100B+ tokens
3. Data Cleaning & Processing
3.1 Cleaning Steps
Remove HTML tags
Remove duplicates
Remove low-quality text
Language filtering
3.2 Deduplication
Exact match
MinHash / LSH (for near-duplicates)
3.3 Normalization
Unicode normalization
Lowercasing (optional)
Remove noise
4. Tokenization
4.1 Choose Tokenizer Type
BPE
SentencePiece (recommended)
WordPiece
4.2 Train Tokenizer

Example using SentencePiece:

import sentencepiece as spm

spm.SentencePieceTrainer.train(
    input='dataset.txt',
    model_prefix='tokenizer',
    vocab_size=32000,
    model_type='bpe'
)
4.3 Tokenizer Design Decisions
Vocabulary size: 32k–100k
Special tokens:
<pad>
<bos>
<eos>
<unk>
5. Model Architecture
5.1 Transformer (Decoder-only GPT)

Core components:

Embedding layer
Positional encoding
N Transformer blocks
5.2 Transformer Block

Each block contains:

Multi-head self-attention
Feedforward network
Layer normalization
Residual connections
5.3 Attention Formula

Attention(Q,K,V)=softmax(
d
k
	​

	​

QK
T
	​

)V

5.4 Model Hyperparameters
Parameter	Typical Value
Layers	12 – 48
Hidden size	768 – 4096
Heads	12 – 32
Context length	512 – 8192
6. Model Implementation (PyTorch)
import torch
import torch.nn as nn

class TransformerBlock(nn.Module):
    def __init__(self, dim, heads):
        super().__init__()
        self.attn = nn.MultiheadAttention(dim, heads)
        self.ff = nn.Sequential(
            nn.Linear(dim, dim*4),
            nn.GELU(),
            nn.Linear(dim*4, dim)
        )
        self.ln1 = nn.LayerNorm(dim)
        self.ln2 = nn.LayerNorm(dim)

    def forward(self, x):
        attn_out, _ = self.attn(x, x, x)
        x = x + attn_out
        x = self.ln1(x)
        ff_out = self.ff(x)
        x = x + ff_out
        x = self.ln2(x)
        return x
7. Training Objective
7.1 Language Modeling Loss

L=−∑
t
	​

logP(x
t
	​

∣x
<t
	​

)

Predict next token
Use Cross Entropy Loss
8. Training Pipeline
8.1 Steps
Load dataset
Tokenize
Create batches
Forward pass
Compute loss
Backpropagation
Update weights
8.2 Training Loop (Simplified)
for batch in dataloader:
    optimizer.zero_grad()
    outputs = model(batch)
    loss = criterion(outputs, targets)
    loss.backward()
    optimizer.step()
9. Optimizers & Learning Rate
9.1 Optimizer
AdamW (standard for LLMs)
9.2 Learning Rate Schedule
Warmup + cosine decay
10. Scaling Training
10.1 Techniques
Data Parallelism
Model Parallelism
Gradient Accumulation
10.2 Mixed Precision
FP16 / BF16
Reduces memory and speeds up training
11. Hardware Requirements
Model Size	GPUs
100M	1 GPU
1B	2–4 GPUs
7B	8+ GPUs
12. Training Stability Tricks
Gradient clipping
Proper initialization
Learning rate warmup
Dropout
13. Evaluation
13.1 Metrics
Perplexity
Loss curve
14. Fine-Tuning
14.1 Methods
Full fine-tuning
LoRA
QLoRA
15. LoRA Concept
Freeze base model
Train low-rank matrices
16. Quantization
INT8 / INT4
Reduce memory
Faster inference
17. Inference Optimization
KV Cache
Flash Attention
Batch inference
18. Deployment
18.1 APIs
FastAPI
Flask
18.2 Serving Engines
vLLM
TensorRT
ONNX Runtime
19. Advanced Topics
19.1 MoE (Mixture of Experts)
Sparse activation
Large models with lower cost
19.2 RAG
Combine LLM + database
19.3 Alignment
RLHF
DPO
20. Full Pipeline Summary
Collect data
Clean & preprocess
Train tokenizer
Design transformer
Train model
Evaluate
Fine-tune
Optimize
Deploy
21. Practical Tips
Start with small model (100M–300M)
Use high-quality data > large noisy data
Monitor loss constantly
Save checkpoints frequently
22. Common Mistakes
Bad tokenizer
Poor data quality
Too high learning rate
No warmup
Small dataset
23. Suggested Stack
PyTorch
HuggingFace Transformers
DeepSpeed / FSDP
SentencePiece
24. Final Notes

Training an LLM from scratch requires:

Strong data pipeline
Efficient compute usage
Careful hyperparameter tuning
Iterative experimentation